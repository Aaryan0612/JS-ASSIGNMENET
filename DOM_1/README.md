# ❤️ Favorite Icon Toggler (DOM Lab 1)

This project demonstrates how to manipulate the DOM using JavaScript click events to toggle the appearance of a favorite icon (heart).

## 📂 Project Structure

- **`index.html`**: Contains the unordered list of items and the button elements.
- **`style.css`**: Defines the styles for the list and the `.filled` class for the active heart state.
- **`script.js`**: Handles the click events to toggle the icon between an empty heart (`♡`) and a filled heart (`❤`).

## 🚀 How to Run

1.  Navigate to the `DOM_1` folder.
2.  Double-click `index.html` to open it in your web browser.

## ✨ Features

- **Interactive Buttons**: Click on the heart icon next to any item.
- **Visual Feedback**: The heart changes color (red) and fills up when selected.
- **Toggle Logic**: Clicking again reverts it to the empty state.
